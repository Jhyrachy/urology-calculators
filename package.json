{
  "name": "urology-calculators",
  "version": "1.0.0",
  "description": "Evidence-based clinical calculators for urology practice",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.4.0"
  }
}